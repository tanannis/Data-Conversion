{
	"name": "data-conversion",
	"version": "1.0.0",
	"description": "converting data from CSV to JSON, then covert back to CSV",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Annis Tan",
	"license": "MIT",
	"dependencies": {
		"convert-csv-to-json": "^1.3.3",
		"json2csv": "^5.0.7"
	}
}
